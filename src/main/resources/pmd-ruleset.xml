<?xml version="1.0"?>
<ruleset name="XStr.me API Specification PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 
         https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        Custom PMD rules for XStr.me API Specification
        Based on best practices and security guidelines
    </description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <exclude name="JUnitTestsShouldIncludeAssert"/>
        <exclude name="AccessorClassGeneration"/>
        <exclude name="GuardLogStatement"/>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="OnlyOneReturn"/>
        <exclude name="AtLeastOneConstructor"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="DefaultPackage"/>
        <exclude name="CallSuperInConstructor"/>
        <exclude name="AbstractNaming"/>
        <exclude name="LongVariable"/>
        <exclude name="ShortVariable"/>
        <exclude name="ShortMethodName"/>
        <exclude name="AvoidFinalLocalVariable"/>
        <exclude name="LocalVariableCouldBeFinal"/>
        <exclude name="MethodArgumentCouldBeFinal"/>
        <exclude name="UnnecessaryLocalBeforeReturn"/>
    </rule>

    <!-- Customize variable name length rules -->
    <rule ref="category/java/codestyle.xml/LongVariable">
        <properties>
            <property name="minimum" value="25"/>
        </properties>
    </rule>

    <rule ref="category/java/codestyle.xml/ShortVariable">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>

    <rule ref="category/java/codestyle.xml/ShortMethodName">
        <properties>
            <property name="minimum" value="2"/>
        </properties>
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
        <exclude name="UseUtilityClass"/>
    </rule>

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <exclude name="CommentRequired"/>
        <exclude name="CommentSize"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <exclude name="BeanMembersShouldSerialize"/>
        <exclude name="DataflowAnomalyAnalysis"/>
        <exclude name="AvoidLiteralsInIfCondition"/>
        <exclude name="NullAssignment"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml">
        <exclude name="UseConcurrentHashMap"/>
    </rule>

    <!-- Performance -->
    <rule ref="category/java/performance.xml">
        <exclude name="AvoidInstantiatingObjectsInLoops"/>
    </rule>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>

    <!-- Custom rules for utility classes -->
    <rule name="UtilityClassShouldBePrivate"
          language="java"
          message="Utility classes should not have public or default constructor"
          class="net.sourceforge.pmd.lang.rule.XPathRule">
        <description>
            Utility classes should not have public or default constructors.
        </description>
        <priority>3</priority>
        <properties>
            <property name="xpath">
                <value>
<![CDATA[
//ClassOrInterfaceDeclaration[@Final='true' and @Utility='true']
[not(ClassOrInterfaceBody/ClassOrInterfaceBodyDeclaration/ConstructorDeclaration[@Private='true'])]
]]>
                </value>
            </property>
        </properties>
    </rule>

    <!-- Custom rule for API spec versioning -->
    <rule name="VersionShouldNotBeHardcoded"
          language="java"
          message="Version strings should not be hardcoded, use properties or manifest"
          class="net.sourceforge.pmd.lang.rule.XPathRule">
        <description>
            Version information should be read from properties or manifest, not hardcoded.
        </description>
        <priority>3</priority>
        <properties>
            <property name="xpath">
                <value>
<![CDATA[
//PrimaryExpression[PrimaryPrefix/Literal[@StringLiteral='true']]
[contains(@Image, '.')]
[matches(@Image, '^\d+\.\d+\.\d+')]
]]>
                </value>
            </property>
        </properties>
    </rule>

    <!-- Exclusions for test files -->
    <exclude-pattern>.*/src/test/.*</exclude-pattern>

    <!-- Custom exclusions for API specification -->
    <exclude-pattern>.*/generated/.*</exclude-pattern>
    <exclude-pattern>.*/target/.*</exclude-pattern>

</ruleset>
